---
'@lorion-org/registry-hub': patch
---

Prepare the registry hub package for first public publication with package metadata, publish-facing package checks, coverage, and consumer-facing examples.

The package exposes framework-free registry and registry-hub primitives with deterministic overwrite behavior, typed entries, enumeration support, examples, and public README guidance.
