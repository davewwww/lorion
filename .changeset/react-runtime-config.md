---
'@lorion-org/react': minor
---

Add scoped runtime-config support for React capability applications, including Vite virtual modules, configurable var-dir loading, server-only private config guards, schema parse errors, and React context helpers for public capability config.
