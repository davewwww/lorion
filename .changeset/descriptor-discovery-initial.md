---
'@lorion-org/descriptor-discovery': minor
---

Introduce the Node-side descriptor discovery package for reading descriptor documents from disk and normalizing them into the flat descriptor shape consumed by `@lorion-org/composition-graph`.

The package includes descriptor schema exports, one-level nested descriptor flattening, id and version normalization, package tests, documentation, and package-check support for public publication.
